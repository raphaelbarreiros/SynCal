# SynCal environment example (do not use in production as-is)

# Environment
NODE_ENV=development

# Service URLs
APP_BASE_URL=http://localhost:3000
API_BASE_URL=http://localhost:3001

# Server ports (adjust to your setup)
PORT_WEB=3000
PORT_API=3001

# Session & crypto
# Generate strong values, e.g., `openssl rand -base64 32`
SESSION_SECRET=change-me-session-secret
ENCRYPTION_MASTER_KEY=base64:CHANGE_ME_32_BYTES

# Database (example for Docker Compose Postgres)
DATABASE_URL=postgres://syncal:syncal@localhost:5432/syncal

# Initial admin bootstrap (first run)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-strong

# CORS / cookies
CORS_ALLOWED_ORIGIN=http://localhost:3000
SESSION_COOKIE_NAME=syn_session

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback
GOOGLE_OAUTH_SCOPES="openid email profile https://www.googleapis.com/auth/calendar"

# Microsoft OAuth (Graph)
MS_CLIENT_ID=
MS_CLIENT_SECRET=
MS_TENANT_ID=common
MS_REDIRECT_URI=http://localhost:3001/auth/microsoft/callback
MS_OAUTH_SCOPES="openid email profile offline_access Calendars.ReadWrite"

# Logging
LOG_LEVEL=info

# SMTP / Email (optional for alerts and notifications)
# For local development, tools like MailHog can be used (e.g., SMTP_HOST=localhost, SMTP_PORT=1025)
# Gmail SMTP (small/self-hosted setups):
#  - Requires 2FA + App Password on the account
#  - SMTP_HOST=smtp.gmail.com, SMTP_PORT=465 (SSL) or 587 (STARTTLS)
#  - SMTP_FROM must match or be allowed by the account domain
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
